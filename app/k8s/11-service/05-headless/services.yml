---
apiVersion: v1
kind: Service
metadata:
  name: postgresql-headless
  namespace: ${NAMESPACE}-db
spec:
  selector:
    app: postgresql
  ports:
  - port: 5432
    targetPort: 5432
  clusterIP: None